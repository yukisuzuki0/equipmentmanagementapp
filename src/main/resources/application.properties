# MySQL設定（コメントアウト）
#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/equipmentdb2?useSSL=false&serverTimezone=Asia/Tokyo}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:teamkaihatsu}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# H2インメモリデータベース設定
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# パフォーマンス最適化設定
spring.jpa.properties.hibernate.jdbc.batch_size=30
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.batch_versioned_data=true
spring.jpa.properties.hibernate.query.in_clause_parameter_padding=true
spring.jpa.properties.hibernate.query.fail_on_pagination_over_collection_fetch=true
spring.jpa.properties.hibernate.default_batch_fetch_size=20

# コネクションプールの設定
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.connection-timeout=30000

# ポート設定（Elastic Beanstalk用に環境変数から取得）
# 注意: Elastic Beanstalkはデフォルトで5000番ポートを使用します
server.port=${PORT:5000}
